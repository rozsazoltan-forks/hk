#!/usr/bin/env bash
#MISE depends=["build", "test:bats:setup"]
#MISE env = { "HK_LIBGIT2" = "" }
#USAGE arg "<test>..." default="test"

set -eu

tests=$(rg --files-without-match git "${usage_test}" --glob "test/*.bats" || true | tr '\n' ' ')
if [ "$tests" ]; then
  bats --jobs 16 $tests
fi
